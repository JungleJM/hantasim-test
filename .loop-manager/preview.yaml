preview:
  kind: native-desktop
  install: "cargo fetch"
  build: "cargo build"
  serve: "cargo run"
  port: null
  public_base_url: ""

proof:
  required: true
  preferred:
    - asciinema
    - screenshot
    - short-screen-recording
  not_applicable_requires_reason: true
